generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model mercy_parkour_codes {
  Map_Number    Int     @default(autoincrement())
  Map           String?
  Code          String  @id
  Checkpoints   Int?
  Video         String?
  Notes         String?
  Author        String?
  Difficulty    String?
  Sit           String?
  Cloud         String?
  Stuck_Balance String?
  Softlock      String?
  Many_Orbs     String?
}
